$("#relationships").html("#{ j(render partial: 'relationships/follow', locals: { user: @user })}");
console.log("aa");
-# $('#relationships').html("#{ j(render 'relationships/follow', user: @user) }");

-# = render 'relationships/follow', user: @user
-# $('#comment_area').html("#{ j(render 'comments/comment') }");