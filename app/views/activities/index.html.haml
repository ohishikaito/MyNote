-# -# = link_to "通知", notifications_path
-# -# .col-md-6.mx-auto
-# -#   - if @activities.exists?
-# -# - @activities.each do |activity|
-# -#   = render '/activities/activities', activity: activity
-# -#   - else
-# -#     %p.text-center
-# -#       アクティビティはありません
-# .wrapper
-#   .container
-#     .notification-top
-#       .notification-top__title
-#         = link_to notifications_path, class: 'notification-top__title__left notification-top__title--hide' do
-#           %h2.notification-top__title--name
-#             通知
-#         .notification-top__title__right.notification-top__title--show
-#           %h2.notification-top__title--name
-#             自分の活動
-#       %ul.notification-top__activities
-#         - if @activities.exists?
-#           -# = render @activities
-#           - @activities.each do |activity|
-#             = render '/activities/activities', activity: activity
-#           -# = paginate @activities
-#         - else
-#           %p.text-center
-#             通知はありません
