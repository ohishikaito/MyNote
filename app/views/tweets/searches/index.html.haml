.wrapper
  - if @tweets.present?
    %ul.tweet-index
      = render partial: 'tweets/tweet', collection: @tweets
  - else
    .text-center
      一致する投稿はありませんでした
    .tweet-index--footer
      = paginate @tweets
= render 'tweets/new_btn'