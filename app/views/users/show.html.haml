.main
  .tweets
    = render "relationships/follow"
    = render "contact"
    = link_to "いいね！した投稿", likes_user_path
    投稿一覧
    .tweets__list
      = render 'tweets/tweet'
    = link_to "フォロー", following_user_path(@user)
    = @user.following.count
    = link_to "フォロワー", followers_user_path(@user)
    = @user.followers.count
    - if current_user == @user 
      = link_to "タイムライン", timeline_user_path(@user)
-# - @users1.each do |user|
-#   = user.nickname
-# - @roomId.each do |u|
-#   = u